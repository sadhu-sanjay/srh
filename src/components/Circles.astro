<!-- class="sticky -top-[140dvh] h-[140dvh] w-full translate-x-20  opacity-0 transition-all delay-700 duration-1000" -->
<div
  class=" sticky h-[140dvh] w-full transition-all delay-700 duration-1000"
  id="circleSvg"
>
  <svg
    xmlns="http://www.w3.org/2000/svg"
    viewBox="0 0 200 200"
    width="100%"
    height="100%"
  >

    <defs>
      <filter id="shadow" x="-50%" y="-50%" width="200%" height="200%">
        <feDropShadow
          dx="0"
          dy="0"
          stdDeviation="5"
          flood-color="#000000"
          flood-opacity="0.2"></feDropShadow>
      </filter>
    </defs>

    <circle cx="120" cy="90" r="100" fill-opacity="0.2"  fill="#fee2e2" ></circle>

    <circle cx="120" cy="90" r="85" fill-opacity="0.2"   fill="#fecaca"></circle>

    <circle cx="120" cy="90" r="70" fill-opacity="0.2"   fill="#fca5a5"></circle>

    <circle cx="120" cy="90" r="55" fill-opacity="0.2"   fill="#f87171"></circle>

    <circle cx="120" cy="90" r="40" fill-opcaity="0.2"  fill="#ec4899"></circle>

    <circle cx="120" cy="90" r="25"  fill="rgb(var(--accent))" > </circle>

  </svg>

  <!-- <div
    id="circle1"
    class="absolute left-1/2 -translate-x-1/2  aspect-square h-full overflow-clip rounded-full bg-red-100 transition-all duration-700"
  >
    <div
      id="circle2"
      class="absolute left-1/2 top-1/2 aspect-square h-[85%] -translate-x-1/2 -translate-y-1/2 rounded-full bg-red-200 transition-all duration-700"
    >
    </div>
    <div
      id="circle3"
      class="absolute left-1/2 top-1/2 aspect-square h-[70%] -translate-x-1/2 -translate-y-1/2 rounded-full bg-red-300 transition-all duration-700"
    >
    </div>
    <div
      id="circle3"
      class="absolute left-1/2 top-1/2 aspect-square h-[55%] -translate-x-1/2 -translate-y-1/2 rounded-full bg-red-400 transition-all duration-700"
    >
    </div>
    <div
      id="circle3"
      class="absolute left-1/2 top-1/2 aspect-square h-[40%] -translate-x-1/2 -translate-y-1/2 rounded-full bg-red-500 transition-all duration-700"
    >
    </div>
    <div
      id="circle3"
      class="absolute left-1/2 top-1/2 aspect-square h-[25%] -translate-x-1/2 -translate-y-1/2 rounded-full bg-red-600 transition-all duration-700"
    >
    </div>
  </div> -->

  <h1
    class=" absolute left-1/2 top-0 w-[100dvw] -translate-x-1/2 text-[35dvw] font-black leading-none text-secondary-accent"
  >
    F<span class="relative inline-block"
      >A<div
        class="absolute left-0 top-[87%] w-auto text-8xl font-black leading-none text-secondary-accent"
      >
        BUILDERS
        <div
          class="leading-2  w-5/6 text-xl font-normal text-secondary-accent"
        >
          SRH spans entertainment and advertising, giving you access to top
          talent and big ideas. <br />
          <br /> We believe that big, bold stunts can be the most efficient way to
          build mental availability (1).
        </div>
      </div></span
    >ME
  </h1>

  <script>
    document.addEventListener("DOMContentLoaded", function () {
      const svgElement = document.getElementById("circleSvg") as HTMLElement;

      // Create an IntersectionObserver
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              // Add 'center' class when the element is in view
              svgElement.classList.add("center");
            } else {
              // Remove 'center' class when it's out of view
              svgElement.classList.remove("center");
            }
          });
        },
        { threshold: 0.5 }
      ); // Adjust threshold as needed

      // Observe the SVG element
      observer.observe(svgElement);
    });
  </script>

  <!--  -->
</div>
