---
const { title, href, classname } = Astro.props;
---

<a href={href}>
  <button
    class=` ${classname} pointer-events-auto rounded-full border-4 border-accent px-8 py-4 text-3xl font-medium text-accent hover:text-white`
  >
    {title}
  </button>
</a>
